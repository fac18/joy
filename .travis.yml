language: node_js
node_js:
  - 'node'
script:
  - cd client && npm i && npm run test -- --coverage --watchAll=false  && cd ../server && npm i && npm test  -- --coverage --watchAll=false
after_success:
  - npm run coverage
