import React from "react";
import { render } from "@testing-library/react";
import ClientProfile from "./ClientProfile";

test("renders the client profile component", () => {
  render(<ClientProfile />);
});
